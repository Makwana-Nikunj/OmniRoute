/**
 * Memory extraction — stubs for lean gateway
 */

/** No-op fact extraction — memory is disabled in lean gateway */
export function extractFacts(_response: string, _apiKeyId: string, _sessionId: string): void {
  // intentionally empty
}
